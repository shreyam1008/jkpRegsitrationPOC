# Backend gRPC-web proxy URL — baked into the bundle at build time
# Cloudflare tunnel routes registrationpocapi.shreyam1008.com.np → http://server:8080
VITE_GRPC_URL=https://registrationpocapi.shreyam1008.com.np
